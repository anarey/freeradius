# -*- text -*-
##
## kafka_log.conf -- Apache kafka modules
##
##	$Id$

######################################################################
#
#  Configuration for the KAFKA module
#

kafka_log {
	# Connection info:
	brokers = "kafka"
	#port = 9092
	topic  = "radius5"
	enrichment.localhost = "{\"key and key\":\"value and value\"}"
#	rdkafka.debug = "all"
	rdkafka.socket.keepalive.enable = "true"
#	rdkafka.socket.max.fails        = "3"
}

